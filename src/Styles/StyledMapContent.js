import styled from "styled-components";

const StyledMapContent = styled.div`
  width: 100%;
  height: calc(100% - 25px);

  display: flex;
  flex-direction: column;

  padding: 5px;
`;

export { StyledMapContent };
